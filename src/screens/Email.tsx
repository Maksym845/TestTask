import React from 'react';
import { SafeAreaView } from 'react-native';
import { Form } from 'compoents/Email/Form';

export const Email = () => {
  return (
    <SafeAreaView>
      <Form containerStyle={{ marginHorizontal: 20 }}/>
    </SafeAreaView>
  );
};
